<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category %>
</p>

<p>
  <% if @category.parent %>
    <strong>Parent:</strong>
    <%= @category.parent %>
  <% else %>
    Top Level Category
  <% end %>
</p>

<%= link_to 'Edit', edit_admin_category_path(@category), class: 'btn btn-link' %>
<%= link_to 'Back', admin_categories_path, class: 'btn btn-link' %>
